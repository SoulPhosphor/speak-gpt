name: Apply importance ranking math

on:
  push:
    branches:
      - fix/importance-ratings-ui
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/importance-ratings-ui
          fetch-depth: 0
      - name: Apply signed ranking patch
        run: python3 .github/scripts/apply_importance_ranking_math.py
      - name: Commit patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git commit -m "fix: apply signed importance ranking semantics"
          git push origin HEAD:fix/importance-ratings-ui
