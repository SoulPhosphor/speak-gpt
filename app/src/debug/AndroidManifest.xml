<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <provider
            android:name="org.teslasoft.assistant.preferences.backup.RecoveryTestDocumentsProvider"
            android:authorities="${applicationId}.recovery.documents"
            android:enabled="${recoveryTestDocumentsProviderEnabled}"
            android:exported="true"
            android:grantUriPermissions="true" />
    </application>
</manifest>
