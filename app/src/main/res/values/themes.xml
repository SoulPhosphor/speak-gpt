<resources>
    <!-- Base application theme. -->
    <style name="Theme.App" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/accent_500</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/accent_100</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="colorAccent">@color/accent_900</item>
        <item name="appRowTitleColor">@color/text</item>
        <item name="appRowSubtitleColor">@color/text_subtitle</item>
        <item name="appTextColor">@color/text</item>
        <item name="appSubtleTextColor">@color/text_subtitle</item>
        <item name="appTitleTextColor">@color/text_title</item>
        <item name="appDropdownBorderColor">@color/dropdown_border</item>
        <item name="appDropdownBackgroundColor">@color/window_background</item>
        <item name="appDropdownTextColor">@color/text</item>
        <item name="appDropdownLabelColor">?attr/colorPrimary</item>
        <item name="appDropdownDisabledTextColor">@color/text_subtitle</item>
        <item name="appDropdownDisabledBorderColor">@color/dropdown_disabled_border</item>
        <item name="android:windowContentTransitions">true</item>
        <item name="android:statusBarColor">@color/window_background</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@color/window_background</item>
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:typeface">sans</item>
        <item name="android:fontFamily">@font/roboto_ttf</item>
        <item name="bottomSheetDialogTheme">@style/ThemeOverlay.App.BottomSheetDialog</item>
        <!-- Owner ruling (July 2026): buttons are semi-square, not pills. This
             is the single shared shape for every default MaterialButton (Save,
             Add, etc.) across the app; buttons that set an explicit style keep
             theirs. -->
        <item name="materialButtonStyle">@style/App.Button</item>
    </style>

    <!-- Default filled button, reshaped to 4dp corners (owner: no pills). -->
    <style name="App.Button" parent="Widget.Material3.Button">
        <item name="shapeAppearanceOverlay">@style/ShapeAppearance.App.Button</item>
    </style>

    <style name="ShapeAppearance.App.Button" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">4dp</item>
    </style>

    <!-- Canonical app buttons. These preserve the current Material 3 appearance,
         use theme color roles automatically, and expose one named text appearance
         plus one shared corner definition for future app-wide changes. -->
    <style name="ShapeAppearance.AppButton" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">@dimen/button_corner_radius</item>
    </style>

    <style name="TextAppearance.App.Button" parent="TextAppearance.Material3.LabelLarge" />

    <!-- Filled current button appearance. -->
    <style name="AppButton.Primary" parent="Widget.Material3.Button">
        <item name="shapeAppearanceOverlay">@style/ShapeAppearance.AppButton</item>
        <item name="android:textAppearance">@style/TextAppearance.App.Button</item>
    </style>

    <!-- No distinct Secondary appearance is approved yet (owner ruling, July 29
         2026): ignore the missing style and inherit Primary's exact appearance.
         New secondary-role buttons should reference AppButton.Primary (and its
         Dialog/DialogAction/Inline variants) directly; this style is kept only
         so a stray reference to it is never visually orphaned. Changeable
         later — that is the point of the ruling. -->
    <style name="AppButton.Secondary" parent="AppButton.Primary" />

    <!-- Same ruling (owner, July 29 2026): Destructive inherits Primary's exact
         appearance until a distinct treatment is approved. Kept as its own
         name, not a direct reference to AppButton.Primary in layouts, so
         destructive buttons can be redesigned app-wide later without touching
         every layout that uses one. -->
    <style name="AppButton.Destructive" parent="AppButton.Primary" />

    <!-- Dialog action button. Identical to AppButton.Primary in every respect
         (fill, corner, text appearance, theme color roles) EXCEPT width: a
         full-width (match_parent) primary button is too wide inside a dialog,
         so this variant sizes to a percentage of the dialog content width
         rather than a fixed pixel/dp value (owner rule, July 19 2026 — do
         percentage, not hard px). The width lives here in ONE place; change
         layout_constraintWidth_percent to reshape every dialog action button.
         Requires a ConstraintLayout parent (the width is 0dp + percent, with
         the button centred to its parent) — used by dialog_single_action.xml. -->
    <style name="AppButton.Primary.Dialog" parent="AppButton.Primary">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="layout_constraintWidth_default">percent</item>
        <item name="layout_constraintWidth_percent">0.6</item>
        <item name="layout_constraintStart_toStartOf">parent</item>
        <item name="layout_constraintEnd_toEndOf">parent</item>
        <item name="layout_constraintTop_toTopOf">parent</item>
        <item name="layout_constraintBottom_toBottomOf">parent</item>
    </style>

    <!-- Two-button dialog action row (owner ruling, July 20 2026): a real
         AppButton.Primary button and a real AppButton.Destructive button
         side by side, first/start = primary, second/end = destructive (e.g.
         Discard Changes: "Okay" first, "Cancel" second) — never flat text
         buttons. Same percentage-width idea as AppButton.Primary.Dialog but
         for two buttons instead of one centered button, so each is 0dp width
         sized by a shared percent. The chain between the two buttons needs
         both of their ids, so it can't be baked into a style — it lives in
         dialog_two_actions.xml; only width/height/appearance live here.
         Change layout_constraintWidth_percent on BOTH styles together to
         reshape the pair. -->
    <style name="AppButton.Primary.DialogAction" parent="AppButton.Primary">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="layout_constraintWidth_default">percent</item>
        <item name="layout_constraintWidth_percent">0.42</item>
    </style>

    <style name="AppButton.Destructive.DialogAction" parent="AppButton.Destructive">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="layout_constraintWidth_default">percent</item>
        <item name="layout_constraintWidth_percent">0.42</item>
    </style>

    <!-- Inline action button (owner ruling, July 24 2026): a primary button
         that sizes to its OWN text (wrap_content width) so a pair can sit
         side by side, each only as wide as its label — deliberately NOT equal
         widths and NOT full-width. Used for the per-database Attempt Repair /
         Revert buttons that appear under a corrupted database's row on the
         Backup & Restore screen. Based on AppButton.Primary; only the width
         differs (owner: "still based off primary, it's just the size width").
         A LinearLayout parent supplies the horizontal spacing between the
         pair (layout_marginEnd in the layout), so no constraint fields here. -->
    <style name="AppButton.Primary.Inline" parent="AppButton.Primary">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <!-- The outlined counterpart of AppButton.Primary.Inline, for a
         wrap_content pair where one action commits and the other backs out
         (Cancel + Save on the include Edit dialog). Same width rule, same
         AppButton.Destructive outlined look. -->
    <style name="AppButton.Destructive.Inline" parent="AppButton.Destructive">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <!-- Shared removable token used when a form turns a selected value into
         an individually removable chip. Model Rule targets and tags use the
         same geometry; the target chip may additionally show ic_report when
         its exact endpoint/model identity was unavailable in the last scan. -->
    <style name="Widget.App.Chip.Removable" parent="Widget.Material3.Chip.Input">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="chipMinHeight">40dp</item>
        <item name="chipStartPadding">8dp</item>
        <item name="chipEndPadding">8dp</item>
        <item name="textStartPadding">4dp</item>
        <item name="textEndPadding">4dp</item>
    </style>

    <!-- ===== Includes strip (document/image attachments) =====
         The row that sits above the message box for every attached item, and
         its pieces. A NEW row shape, deliberately not one of the five
         chevron-row shapes in ui-style-guide.md: it has no chevron and ends
         in a state-specific attachment action instead, so extending the
         chevron-row vocabulary would have been wrong. Typography/colour roles
         are borrowed from the row styles so the strip reads as part of the
         same system.
         See document-includes-plan.md for the owner-approved design. -->
    <style name="Widget.App.Include.Container" parent="">
        <item name="android:background">@drawable/bg_attachment_tile</item>
    </style>

    <style name="Widget.App.Include.Row" parent="">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:orientation">horizontal</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:minHeight">44dp</item>
        <item name="android:paddingStart">12dp</item>
        <item name="android:paddingEnd">4dp</item>
        <item name="android:paddingTop">4dp</item>
        <item name="android:paddingBottom">4dp</item>
    </style>

    <!-- Leading file-type icon. Each supported type has its own glyph so a
         later image/other type is identifiable at a glance (owner
         requirement). No tint here — instances tint ?attr/colorPrimary,
         leaving the slot open for a future non-glyph thumbnail. -->
    <style name="Widget.App.Include.Icon" parent="">
        <item name="android:layout_width">24dp</item>
        <item name="android:layout_height">24dp</item>
        <item name="android:layout_marginEnd">10dp</item>
    </style>

    <!-- "Includes" / "Includes condensed" — the label that states which form
         of the item is being sent right now. -->
    <style name="Widget.App.Include.Label" parent="">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginEnd">6dp</item>
        <item name="android:textColor">?attr/colorPrimary</item>
        <item name="android:textSize">13sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="Widget.App.Include.Name" parent="">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:maxLines">1</item>
        <item name="android:ellipsize">middle</item>
        <item name="android:textColor">?attr/appTextColor</item>
        <item name="android:textSize">14sp</item>
    </style>

    <!-- The "~N tokens" weight readout. Subtitle colour/size so it reads as
         secondary information next to the file name. -->
    <style name="Widget.App.Include.Weight" parent="">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginStart">8dp</item>
        <item name="android:maxLines">1</item>
        <item name="android:textColor">?attr/appSubtleTextColor</item>
        <item name="android:textSize">12sp</item>
    </style>

    <style name="Widget.App.Include.Action" parent="">
        <item name="android:layout_width">40dp</item>
        <item name="android:layout_height">40dp</item>
        <item name="android:background">?attr/selectableItemBackgroundBorderless</item>
        <item name="android:tint">?attr/colorPrimary</item>
        <item name="android:scaleType">centerInside</item>
    </style>

    <!-- Persistent size notice under a row (never a toast, per house rule).
         Same size/colour as the shared field hint. -->
    <style name="Widget.App.Include.Notice" parent="">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingStart">46dp</item>
        <item name="android:paddingEnd">12dp</item>
        <item name="android:paddingBottom">6dp</item>
        <item name="android:textColor">?attr/appSubtleTextColor</item>
        <item name="android:textSize">13sp</item>
    </style>

    <!-- Shared chevron-row styles, preserving the current Memory Manager row
         appearance. Use Widget.App.Row.WithSubtitle when the row has helper text
         and Widget.App.Row.TitleOnly when it does not. Both variants intentionally
         share the same outer geometry; Title and Subtitle own the typography. -->
    <style name="Widget.App.Row" parent="">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">?attr/selectableItemBackground</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:minHeight">56dp</item>
        <item name="android:orientation">horizontal</item>
        <item name="android:paddingVertical">12dp</item>
    </style>

    <style name="Widget.App.Row.WithSubtitle" parent="Widget.App.Row" />

    <style name="Widget.App.Row.TitleOnly" parent="Widget.App.Row" />

    <style name="Widget.App.Row.TextColumn" parent="">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:orientation">vertical</item>
    </style>

    <style name="Widget.App.Row.Title" parent="">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">?attr/appRowTitleColor</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="Widget.App.Row.Subtitle" parent="">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:ellipsize">end</item>
        <item name="android:maxLines">1</item>
        <item name="android:textColor">?attr/appRowSubtitleColor</item>
        <item name="android:textSize">13sp</item>
    </style>

    <style name="Widget.App.Row.Chevron" parent="">
        <item name="android:layout_width">24dp</item>
        <item name="android:layout_height">24dp</item>
        <item name="android:layout_marginStart">12dp</item>
        <item name="android:src">@drawable/ic_chevron_right</item>
        <item name="android:tint">?attr/appRowSubtitleColor</item>
    </style>

    <!-- Leading row icon/avatar (owner request, July 18 2026). Not a new row
         shape — it's one extra piece that composes with the existing
         Widget.App.Row.WithSubtitle / Widget.App.Row.TitleOnly containers as
         the row's first child, before Widget.App.Row.TextColumn. Title and
         subtitle keep using the exact same Widget.App.Row.Title/Subtitle
         styles either way, so a row's text never differs because it has (or
         lacks) a leading icon — there's only one Title style and one
         Subtitle style, period.
         Sized to match the leading identity icon already used on memory rows
         (view_memory_row.xml) for one consistent icon size app-wide.
         Deliberately carries no tint of its own: today's plain vector icons
         set android:tint per instance (matching Widget.App.Row.Chevron's own
         convention), but a future avatar/photo (Characters, Personas) must
         be able to sit in this same slot unforced into a single color, so
         the shared style never bakes one in. -->
    <style name="Widget.App.Row.Icon" parent="">
        <item name="android:layout_width">36dp</item>
        <item name="android:layout_height">36dp</item>
        <item name="android:layout_marginEnd">16dp</item>
        <item name="android:scaleType">centerCrop</item>
    </style>

    <!-- Leading Profile Image preview (profile-images-plan.md, XML STYLE AND
         THEME INTEGRATION REQUIREMENTS: "Widget.App.Row.ProfileImage").
         Completely based on Widget.App.Row.Icon - same slot, same formula -
         and overrides ONLY the size via @dimen/row_profile_image_size, so a
         future change to the shared icon formula (end margin, scale type,
         no-baked-in-tint) flows into both automatically while the two sizes
         stay independent. Used by the Global Default / Personal Default rows
         (Default Images screen) to show the currently assigned picture. -->
    <style name="Widget.App.Row.ProfileImage" parent="Widget.App.Row.Icon">
        <item name="android:layout_width">@dimen/row_profile_image_size</item>
        <item name="android:layout_height">@dimen/row_profile_image_size</item>
    </style>

    <!-- Toggle row (owner request, July 18 2026): a settings row whose
         whole point is flipping a boolean, not navigating anywhere, so it
         has no chevron and always carries a subtitle. Reuses the shared
         Widget.App.Row.Title/Subtitle/TextColumn styles so its text
         matches every other row's typography and inherits future theming
         the same way; only the container geometry here is specific to this
         variant. Matches, unchanged, how the app's one existing toggle row
         (Automatically Apply Model Rules, AI System Settings) already
         looked before conversion — no ripple, no minHeight, not clickable
         itself, since only the switch is the tap target. -->
    <style name="Widget.App.Row.Toggle" parent="">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:orientation">horizontal</item>
        <item name="android:paddingTop">20dp</item>
    </style>

    <!-- Trailing switch for Widget.App.Row.Toggle. -->
    <style name="Widget.App.Row.Switch" parent="">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginStart">12dp</item>
    </style>

    <!-- Selector row (owner request, Aug 13 2026): a labeled row that shows
         the current value of a setting and opens a picker when tapped. The
         "AI Model" row in Quick Settings (unstyled before this) and the
         Voice Language row (Voice & Speech) both use this now, unchanged in
         appearance either way. No chevron (unlike Widget.App.Row.*): the
         current value itself, right-aligned, is the affordance. Distinct
         from the Dropdown family, which opens an anchored inline menu
         instead of a separate picker screen/dialog. The background is the
         tonal pill (@drawable/btn_accent_tonal, tinted
         colorSecondaryContainer); a screen that groups a Selector row
         under/above other card content may override android:background
         with a differently-cornered drawable (Quick Settings keeps
         @drawable/btn_accent_top, rounded only on top, so the row still
         reads as the top of that settings card) - that is placement only,
         matching how the canonical dropdown allows container differences. -->
    <style name="Widget.App.Row.Selector" parent="">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@drawable/btn_accent_tonal</item>
        <item name="android:backgroundTint">?attr/colorSecondaryContainer</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:paddingTop">14dp</item>
        <item name="android:paddingBottom">14dp</item>
    </style>

    <!-- Same selector-row geometry and typography as AI Model, with a
         distinct theme-resolved background for Voice Language. -->
    <style name="Widget.App.Row.Selector.VoiceLanguage" parent="Widget.App.Row.Selector">
        <item name="android:backgroundTint">?attr/colorSurfaceContainerHigh</item>
    </style>

    <!-- Left-aligned label for Widget.App.Row.Selector. -->
    <style name="Widget.App.Row.Selector.Label" parent="">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginStart">16dp</item>
        <item name="android:textColor">?attr/colorPrimary</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Right-aligned current value for Widget.App.Row.Selector. -->
    <style name="Widget.App.Row.Selector.Value" parent="">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginStart">16dp</item>
        <item name="android:layout_marginEnd">16dp</item>
        <item name="android:ellipsize">marquee</item>
        <item name="android:focusable">true</item>
        <item name="android:focusableInTouchMode">true</item>
        <item name="android:freezesText">true</item>
        <item name="android:gravity">end</item>
        <item name="android:marqueeRepeatLimit">marquee_forever</item>
        <item name="android:scrollHorizontally">true</item>
        <item name="android:singleLine">true</item>
        <item name="android:textColor">?attr/appTextColor</item>
    </style>

    <!-- Flush variant of Widget.App.Row.Selector for plain settings screens.
         Removes the tonal pill background and the label's leading inset so the
         selector reads as an ordinary row: its label starts at the same
         leading edge as the screen's other row text instead of being indented
         like the Quick Settings card version. The screen supplies its own
         horizontal padding to line the label up with its rows; this style only
         drops the pill and the inset. Quick Settings keeps the indented,
         pill-backed base Widget.App.Row.Selector. -->
    <style name="Widget.App.Row.Selector.Flush" parent="Widget.App.Row.Selector">
        <item name="android:background">@null</item>
    </style>

    <!-- Label for Widget.App.Row.Selector.Flush: same bold accent typography
         as Widget.App.Row.Selector.Label, with the leading inset removed so it
         aligns with the screen's other row titles. -->
    <style name="Widget.App.Row.Selector.Label.Flush" parent="Widget.App.Row.Selector.Label">
        <item name="android:layout_marginStart">0dp</item>
    </style>

    <!-- Stacked single-choice radio row for a settings screen: a full-width,
         tappable RadioButton whose label matches the shared row-title
         typography (16sp, appRowTitleColor) and whose control tint resolves
         from the theme (colorPrimary). Put the RadioButtons directly in a
         vertical RadioGroup so it handles single-selection; the group supplies
         the horizontal padding that aligns the rows with the screen's other
         rows. Nothing here is hard-coded in the layout — size, color, and
         geometry all live in this style so a future theme pass changes them in
         one place. -->
    <style name="Widget.App.Row.Radio" parent="Widget.Material3.CompoundButton.RadioButton">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:minHeight">48dp</item>
        <item name="android:paddingTop">8dp</item>
        <item name="android:paddingBottom">8dp</item>
        <item name="android:buttonTint">?attr/colorPrimary</item>
        <item name="android:textColor">?attr/appRowTitleColor</item>
        <item name="android:textSize">16sp</item>
    </style>

    <!-- Pick-one-item list row (owner request, Aug 13 2026): the "checked
         tile" row used by a single-select list opened from a
         Widget.App.Row.Selector (or, later, from any other picker needing
         the same look) - currently the Select Language dialog's
         RadioButton rows. Geometry/typography unchanged from that dialog's
         previous local values (56dp row, 8dp horizontal padding, 16sp). It
         deliberately does not force single-line or ellipsized text.
         The Select AI Model screen's row layout (view_model.xml) still
         improvises its own local version of this same pattern and has not
         been migrated onto this style - see ui-style-adoption.md.
         This style only carries the shared geometry; the two selection
         states are applied at runtime (background + text appearance, see
         TextAppearance.App.PickList.* below) by whatever code owns the
         individual rows:
           - unselected: android:background = @drawable/btn_accent_tonal_selector_v3,
             text appearance = TextAppearance.App.PickList.Unselected
           - selected: android:background = @drawable/btn_accent_tonal_selector_v4,
             text appearance = TextAppearance.App.PickList.Selected
         Neither drawable needs a runtime tint - both already resolve their
         fill from a theme attribute (colorSurfaceContainerHigh / colorPrimary
         respectively); tinting them again in Kotlin is what caused the old
         hard-coded-color bug this style exists to fix. -->
    <!-- Custom pick-list dialog title: existing geometry, theme-routed color. -->
    <style name="Widget.App.PickList.Title" parent="">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">24dp</item>
        <item name="android:textColor">?attr/appTitleTextColor</item>
        <item name="android:textSize">24sp</item>
    </style>

    <style name="Widget.App.PickList.Row" parent="">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">56dp</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:paddingStart">8dp</item>
        <item name="android:paddingEnd">8dp</item>
        <item name="android:textSize">16sp</item>
    </style>

    <!-- Unselected-state text for Widget.App.PickList.Row: same row-title
         color every other shared row uses. -->
    <style name="TextAppearance.App.PickList.Unselected" parent="">
        <item name="android:textColor">?attr/appRowTitleColor</item>
    </style>

    <!-- Selected-state text for Widget.App.PickList.Row: resolved through an
         XML text appearance, not a Kotlin attribute lookup, so it never hits
         the colorOnPrimary CI resolution risk recorded elsewhere in this
         project (see ProfileImageGalleryAdapter.kt / MemoryScreenActivity.kt). -->
    <style name="TextAppearance.App.PickList.Selected" parent="">
        <item name="android:textColor">?attr/colorOnPrimary</item>
    </style>

    <!-- Screen header (owner ruling, July 19 2026): the solid-color bar with
         a back chevron and a centered title at the top of a full-screen
         activity (seen today on Characters, AI System Settings, and
         others). Until now this was hand-copied XML with no shared style,
         which is how new screens silently drifted from it. Font, size,
         padding, margins and positioning for the whole header now live
         here - change them once and every screen using these styles
         follows. A screen only needs an id on each piece (plus its own
         title text) - everything else, including the ConstraintLayout
         pin-to-parent/centering that makes this portable across screens
         without per-instance constraint XML, is baked in below.
         Deliberately excludes the old per-Activity AMOLED pitch-black
         recolor logic (dynamic background/back-button/status-bar
         recoloring in Kotlin) - the owner says that mechanism is being
         removed and does not want it carried into the new style.
         Rolled out (July 19 2026) to ProfileImagePropertiesActivity,
         DefaultImagesActivity, CharactersActivity, PersonasListActivity
         ("Companions"), ActivationPromptsListActivity, and - via the
         shared activity_memory_list_simple.xml scaffold plus
         MemoryScreenActivity.contentLayoutRes() - MemoryUserPersonasActivity
         ("My Personas"), MemoryRoleplayCharactersActivity, MemoryWorldsActivity,
         MemoryPartyMembersActivity, MemoryCampaignsActivity and RpTagsActivity
         (the five Roleplay-hub screens). None of those six wire an action
         icon, secondary action, filter or mode toggle, so none of them
         needed the reserved space activity_memory_list.xml's button-aware
         title still gives the OTHER MemoryScreenActivity screens that do
         (e.g. Memory Browser, Model Rules); AlertDebugMenuActivity
         (activity_alert_debug_menu.xml, "Alerts, Errors & Logs"),
         VoiceAdvancedSettingsActivity (activity_voice_advanced.xml,
         "Advanced Voice Settings") and AudioDebuggingActivity
         (activity_audio_debugging.xml, "Voice Debugging" tile); LogsActivity
         (activity_logs.xml, all four log pages) via this trio PLUS
         Widget.App.ActionBar.SecondaryButton below (its Event-log-only
         voice-advanced shortcut icon); SettingsActivity
         (activity_settings.xml, main Settings screen), restructured same
         day so the header pins above the ScrollView instead of scrolling
         away with it (see the CLAUDE.md rollout-status note for the
         restructure details); LocalWhisperManageActivity and
         LocalWhisperModelsActivity (activity_local_whisper_manage.xml/
         activity_local_whisper_models.xml - the old floating-title-no-bar
         look, now gets the colored bar for the first time);
         LoreBookEntriesActivity and LoreBooksListActivity
         (activity_lorebook_entries.xml/activity_lorebooks_list.xml,
         via this trio plus SecondaryButton for their debug/edit-book
         icons - entries has two chained icons, see SecondaryButton's own
         doc comment below for how the inner one is handled); and
         LogitBiasConfigListActivity/LogitBiasConfigActivity
         (activity_logit_bias_config_list.xml/activity_logit_bias_list.xml,
         via this trio plus SecondaryButton for their help icon - also
         dropped a redundant android:backgroundTint on the buttons that
         just re-stated the same colorSurfaceContainerHigh the drawable
         already bakes in) - and VoiceSettingsActivity
         (activity_voice_settings.xml, "Voice & Speech"), restructured the
         same way as Settings so its header pins too (same
         expandable_window slide-in-panel shape, but only one child, so
         the outer LinearLayout converts to ConstraintLayout directly
         instead of needing Settings' extra wrapper). Title size raised
         20sp -> 24sp same day to match the size these screens already
         used before conversion (owner ruling: match the existing look,
         not the smaller size introduced with the first two screens).
         Not yet applied to the rest of the app; keep rolling out screen
         by screen. -->
    <style name="Widget.App.ActionBar" parent="">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">?attr/colorSurfaceContainerHigh</item>
        <item name="layout_constraintEnd_toEndOf">parent</item>
        <item name="layout_constraintStart_toStartOf">parent</item>
        <item name="layout_constraintTop_toTopOf">parent</item>
    </style>

    <!-- The leading back button inside Widget.App.ActionBar. -->
    <style name="Widget.App.ActionBar.BackButton" parent="">
        <item name="android:layout_width">48dp</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:layout_marginStart">8dp</item>
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:layout_marginBottom">8dp</item>
        <item name="android:background">@drawable/btn_accent_icon_large_100</item>
        <item name="android:contentDescription">@string/go_back</item>
        <item name="android:src">@drawable/ic_back</item>
        <item name="android:tooltipText">@string/go_back</item>
        <item name="srcCompat">@drawable/ic_back</item>
        <item name="layout_constraintBottom_toBottomOf">parent</item>
        <item name="layout_constraintStart_toStartOf">parent</item>
        <item name="layout_constraintTop_toTopOf">parent</item>
    </style>

    <!-- The centered title inside Widget.App.ActionBar. Only android:text
         is left for the instance to set. -->
    <style name="Widget.App.ActionBar.Title" parent="">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">?attr/appTitleTextColor</item>
        <item name="android:textSize">24sp</item>
        <item name="android:textStyle">bold</item>
        <item name="layout_constraintBottom_toBottomOf">parent</item>
        <item name="layout_constraintEnd_toEndOf">parent</item>
        <item name="layout_constraintStart_toStartOf">parent</item>
        <item name="layout_constraintTop_toTopOf">parent</item>
    </style>

    <!-- Left-aligned title for any Widget.App.ActionBar header that also has
         one or more trailing icon buttons (owner ruling, July 21 2026):
         the title sits right after the back button with a small gap, not
         centered across the whole bar - matches the look
         activity_memory_list.xml's scaffold already used. NOT a child of
         Widget.App.ActionBar.Title (same reason Title.LeftAligned below
         isn't either: a ConstraintLayout view can't cleanly carry both an
         inherited layout_constraintEnd_toEndOf and an added
         layout_constraintEnd_toStartOf). layout_constraintEnd_toStartOf is
         NOT baked in here - every screen's first trailing icon has a
         different id, so each instance sets its own
         app:layout_constraintEnd_toStartOf pointing at that icon (or, with
         two or more chained icons, the one nearest the title). -->
    <style name="Widget.App.ActionBar.Title.NearBack" parent="">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginStart">8dp</item>
        <item name="android:ellipsize">end</item>
        <item name="android:maxLines">1</item>
        <item name="android:textColor">?attr/appTitleTextColor</item>
        <item name="android:textSize">24sp</item>
        <item name="android:textStyle">bold</item>
        <item name="layout_constraintBottom_toBottomOf">parent</item>
        <item name="layout_constraintHorizontal_bias">0.0</item>
        <item name="layout_constraintStart_toEndOf">@+id/btn_back</item>
        <item name="layout_constraintTop_toTopOf">parent</item>
    </style>

    <!-- THE "icon on top" PATTERN (owner ruling, July 19 2026): use this,
         not raw XML, any time a Widget.App.ActionBar screen needs a
         trailing icon button (a shortcut, a settings gear, etc.) alongside
         the back button - this was the gap that left activity_logs.xml
         hand-copied instead of sharing the header style (see the Logs
         entry below). Same geometry/background as
         Widget.App.ActionBar.BackButton, anchored to the bar's END instead
         of its START. android:src/contentDescription/tooltipText are
         screen-specific (the icon differs per screen) and stay on the
         instance - only geometry/background are shared here. Title stays
         plain Widget.App.ActionBar.Title, unchanged: it's centered across
         the WHOLE bar regardless of the icon's presence (matches how the
         pre-conversion Logs title already floated centered, ignoring the
         back button's position) rather than re-deriving an off-center/
         ellipsized variant - simpler, and it already looked right. For a
         SECOND trailing icon, constrain its layout_constraintEnd_toStartOf
         the first icon instead of parent (see activity_memory_list.xml's
         btn_action/btn_action_secondary chain for that shape) rather than
         inventing a new style per icon count. -->
    <style name="Widget.App.ActionBar.SecondaryButton" parent="">
        <item name="android:layout_width">48dp</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:layout_marginEnd">8dp</item>
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:layout_marginBottom">8dp</item>
        <item name="android:background">@drawable/btn_accent_icon_large_100</item>
        <item name="layout_constraintBottom_toBottomOf">parent</item>
        <item name="layout_constraintEnd_toEndOf">parent</item>
        <item name="layout_constraintTop_toTopOf">parent</item>
    </style>

    <!-- ADDITIONAL trailing header icons (owner approval, July 30 2026): any
         header icon that sits BEFORE the end-anchored SecondaryButton. Same
         geometry/background as SecondaryButton, but with NO end anchor baked
         in — each instance sets app:layout_constraintEnd_toStartOf pointing
         at its right-hand neighbor (each screen's neighbor id differs, same
         reason Title.NearBack leaves its end anchor to the instance). NOT a
         child of SecondaryButton: a view can't cleanly carry both an
         inherited layout_constraintEnd_toEndOf and an added
         layout_constraintEnd_toStartOf (both would apply — ambiguous), the
         same conflict documented on the Title variants above, so the shared
         items are duplicated rather than inherited.
         android:src/contentDescription/tooltipText/visibility stay on the
         instance. This closes the "two or more trailing icons" gap that had
         Edit Companion/Edit Persona/API Endpoint Editor/Edit Activation
         Prompt/Edit System Prompt/Lorebook Entries hand-copying this
         geometry. -->
    <style name="Widget.App.ActionBar.ChainedButton" parent="">
        <item name="android:layout_width">48dp</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:layout_marginEnd">8dp</item>
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:layout_marginBottom">8dp</item>
        <item name="android:background">@drawable/btn_accent_icon_large_100</item>
        <item name="layout_constraintBottom_toBottomOf">parent</item>
        <item name="layout_constraintTop_toTopOf">parent</item>
    </style>

    <!-- THE "close panel" PATTERN (owner ruling, July 19 2026): use this,
         not raw XML, for any slide-out/slide-in panel whose header closes
         with an X instead of navigating back with an arrow - the shape
         first built for the Memory Filters panel
         (activity_memory_filter_panel.xml) and now formalized so the next
         filter-style pop-out reuses it instead of hand-copying. Owner
         ruling: keep this look distinct from the plain back+centered-title
         header rather than forcing it to match - it's a different kind of
         screen (a panel you close, not a screen you navigate back from),
         and the existing look was already liked as-is.
         Container: still plain Widget.App.ActionBar, unchanged - only the
         title and the button differ from the back+title family.
         Widget.App.ActionBar.CloseButton mirrors
         Widget.App.ActionBar.BackButton's geometry (same size/margins/
         background) but anchored to the bar's END, with the close icon
         baked in the same way BackButton bakes in the back icon.
         contentDescription/tooltipText default to the generic
         @string/btn_close ("Close") but - like BackButton's
         @string/go_back - an instance can override them with something
         more specific (Memory Filters uses @string/mem_filter_close,
         "Close Memory Filters", since a screen-reader label naming what's
         closing is more useful than a bare "Close" on an icon-only
         button).
         Widget.App.ActionBar.Title.LeftAligned is the title for this
         family: left-aligned and ellipsized (it shares the row with the
         close button, so it can't center across the whole bar the way the
         plain Title does) at 20sp, not 24sp - a deliberate, smaller size
         for this variant, not a step of the size rollout the plain Title
         went through. It is NOT a child of Widget.App.ActionBar.Title:
         ConstraintLayout can't cleanly have both an inherited
         layout_constraintEnd_toEndOf and an added
         layout_constraintEnd_toStartOf on the same view (both would be
         set, which is ambiguous), so the shared color/weight items are
         duplicated here instead of inherited. This style HARDCODES
         layout_constraintEnd_toStartOf="@+id/btn_close" - any screen using
         it MUST name its close button btn_close (matching how
         Widget.App.ActionBar.BackButton assumes screens name their back
         button btn_back). -->
    <style name="Widget.App.ActionBar.CloseButton" parent="">
        <item name="android:layout_width">48dp</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:layout_marginEnd">8dp</item>
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:layout_marginBottom">8dp</item>
        <item name="android:background">@drawable/btn_accent_icon_large_100</item>
        <item name="android:contentDescription">@string/btn_close</item>
        <item name="android:src">@drawable/ic_close</item>
        <item name="android:tooltipText">@string/btn_close</item>
        <item name="srcCompat">@drawable/ic_close</item>
        <item name="layout_constraintBottom_toBottomOf">parent</item>
        <item name="layout_constraintEnd_toEndOf">parent</item>
        <item name="layout_constraintTop_toTopOf">parent</item>
    </style>

    <style name="Widget.App.ActionBar.Title.LeftAligned" parent="">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginStart">20dp</item>
        <item name="android:layout_marginEnd">8dp</item>
        <item name="android:ellipsize">end</item>
        <item name="android:maxLines">1</item>
        <item name="android:textColor">?attr/appTitleTextColor</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
        <item name="layout_constraintBottom_toBottomOf">parent</item>
        <item name="layout_constraintEnd_toStartOf">@+id/btn_close</item>
        <item name="layout_constraintStart_toStartOf">parent</item>
        <item name="layout_constraintTop_toTopOf">parent</item>
    </style>

    <!-- Shared label-above-box form field (owner ruling, July 20 2026; see
         ui-style-guide.md for the full writeup). First built by hand,
         repeated per field, on the roleplay character card
         (activity_character_card.xml) - these two styles are that same
         look, de-duplicated. Deliberately no Material floating/embedded
         hint anywhere: the field name is always this separate Label
         TextView above the box, never a TextInputLayout hint.
         Widget.App.Field.Box intentionally leaves layout_height as
         wrap_content (not baked to a fixed size) so fields with different
         amounts of text - a one-line name vs. a five-line description -
         share the same skin without being forced to the same height. -->
    <style name="Widget.App.Field.Label" parent="">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">?attr/colorPrimary</item>
        <!-- 16sp matches the app's section-title size (owner ruling, July 20
             2026 — e.g. "Additional lorebooks" on the Edit Companion screen).
             Shared, so every screen using this label style — including the
             roleplay character card — moves together, which is the point. -->
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Optional small explanatory line under a field's Label, above its Box
         (owner ruling, July 20 2026). Not every field has one - omit the
         TextView entirely for a field with no hint, same as omitting
         Widget.App.Row.Subtitle for a row with none. Same size/color this
         app already uses for this kind of secondary text elsewhere (row
         subtitles, the character card's zone subtext). -->
    <style name="Widget.App.Field.Hint" parent="">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">2dp</item>
        <item name="android:textColor">?attr/appSubtleTextColor</item>
        <item name="android:textSize">13sp</item>
    </style>

    <!-- Screen-section heading + its explanatory text (owner ruling, July 22
         2026). Same "heading, then explanation, then the control it explains"
         order as Widget.App.Field.Label/.Hint above, one level up: a whole
         settings-style screen section, not a single form field. A section's
         hint/description text ALWAYS sits directly under that section's
         title and BEFORE any button or control in the section - never under
         or after a button. Text describing what a button does must never sit
         below that button. A section with more than one explanatory sentence
         (e.g. a description plus a separate warning) stacks both as
         consecutive Widget.App.Section.Hint paragraphs directly under the
         title, still before any control - never split apart with a button in
         between. These two styles de-duplicate a title+hint pair that was
         hand-copied per section (Database Health, Recovery Backup,
         Human-Readable Chat Backup, Portable Data Copy, Automatic Backups,
         Reset) in activity_memory_backup_restore.xml with identical values;
         extracting them here is what makes the coming theming pass a
         one-place change instead of a hunt through every section. -->
    <style name="Widget.App.Section.Title" parent="">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingTop">24dp</item>
        <item name="android:textColor">?attr/colorPrimary</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Explanatory text for a section, directly under its Section.Title and
         before that section's first control (see the ordering rule above). -->
    <style name="Widget.App.Section.Hint" parent="">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingTop">8dp</item>
        <item name="android:textColor">?attr/appSubtleTextColor</item>
        <item name="android:textSize">13sp</item>
    </style>

    <!-- Plain standalone paragraph at the top of a screen, with no title of
         its own above it (owner ruling, July 26 2026). Distinct from both
         Section.Title (bold, larger, colorPrimary) and Section.Hint/
         Row.Subtitle (13sp, muted text_subtitle) - this is normal-weight
         body text at full text_title strength, for a screen-level
         explanation that isn't paired with a heading. -->
    <style name="Widget.App.Screen.Intro" parent="">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">?attr/appTitleTextColor</item>
        <item name="android:textSize">14sp</item>
    </style>

    <!-- Chat composer host: the space behind the floating composer stays transparent. -->
    <style name="Widget.App.Chat.ComposerHost" parent="">
        <item name="android:background">@color/transparent</item>
    </style>

    <!-- Chat composer surface: the editor and control row use the same
         theme-resolved surface as the incoming AI bubble. -->
    <style name="Widget.App.Chat.ComposerSurface" parent="">
        <item name="android:background">@drawable/bubble_in</item>
    </style>

    <!-- Bare primary actions in the composer control row. Geometry, icon
         tint, and the absence of a background remain centrally themeable. -->
    <style name="Widget.App.Chat.ComposerAction" parent="">
        <item name="android:layout_width">48dp</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:background">@color/transparent</item>
        <item name="android:scaleType">center</item>
        <item name="android:tint">?attr/colorPrimary</item>
    </style>

    <!-- Shared camera/image/document and conversation-tools pop-up family. -->
    <style name="Widget.App.Chat.ActionMenu.Popup" parent="">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginStart">12dp</item>
        <item name="android:layout_marginBottom">12dp</item>
        <item name="android:elevation">12dp</item>
        <item name="android:orientation">vertical</item>
    </style>

    <style name="Widget.App.Chat.ActionMenu.Card" parent="">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="cardBackgroundColor">@color/transparent</item>
        <item name="cardCornerRadius">24dp</item>
        <item name="strokeWidth">0dp</item>
    </style>

    <style name="Widget.App.Chat.ActionMenu.Surface" parent="">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="blurOverlayColor">@color/blur_background</item>
    </style>

    <style name="Widget.App.Chat.ActionMenu.Content" parent="">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:orientation">vertical</item>
        <item name="android:paddingVertical">6dp</item>
    </style>

    <style name="Widget.App.Chat.ActionMenu.Row" parent="">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">?attr/selectableItemBackground</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:minHeight">48dp</item>
        <item name="android:orientation">horizontal</item>
        <item name="android:paddingHorizontal">16dp</item>
    </style>

    <style name="Widget.App.Chat.ActionMenu.Icon" parent="">
        <item name="android:layout_width">24dp</item>
        <item name="android:layout_height">24dp</item>
        <item name="android:layout_marginEnd">12dp</item>
        <item name="android:importantForAccessibility">no</item>
        <item name="android:tint">?attr/colorPrimary</item>
    </style>

    <style name="Widget.App.Chat.ActionMenu.Label" parent="">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingEnd">8dp</item>
        <item name="android:textColor">?attr/appTextColor</item>
        <item name="android:textSize">16sp</item>
    </style>

    <!-- One manual-compaction boundary, centered between message units. -->
    <style name="Widget.App.Chat.CompactionMarker" parent="Widget.App.Section.Title">
        <item name="android:layout_width">0dp</item>
        <item name="android:gravity">center</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
    </style>

    <!-- One moving automatic-summary boundary in ordinary centered text. -->
    <style name="Widget.App.Chat.SummaryMarker" parent="">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:gravity">center</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
        <item name="android:textColor">?attr/appTextColor</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textStyle">normal</item>
    </style>

    <!-- Bare expand/collapse controls for the live chat composer. Their shared
         style, not the layout or legacy per-screen theme code, owns the icon
         geometry, transparent background, and theme-resolved tint. Placement
         remains local because Expand is in the compact control row while
         Collapse is anchored at the expanded editor's top end. -->
    <style name="Widget.App.Chat.ComposerContentToggle" parent="">
        <item name="android:layout_width">48dp</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:background">@color/transparent</item>
        <item name="android:scaleType">center</item>
        <item name="android:tint">?attr/colorPrimary</item>
    </style>

    <!-- Provider-supplied Thinking disclosure. The layout owns only content,
         ids, state, and constraints; this family owns its presentation so a
         later appearance/theme pass can change it centrally. ChatAdapter may
         replace label/body/chevron colors together with the resolved bubble
         foreground for contrast, using its one shared bubble-foreground path. -->
    <style name="Widget.App.Chat.Thinking.Container" parent="">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginBottom">6dp</item>
        <item name="android:orientation">vertical</item>
    </style>

    <style name="Widget.App.Chat.Thinking.Header" parent="">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">?attr/selectableItemBackgroundBorderless</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:minHeight">32dp</item>
        <item name="android:orientation">horizontal</item>
    </style>

    <style name="Widget.App.Chat.Thinking.Label" parent="">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">?attr/colorPrimary</item>
        <item name="android:textSize">13sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="Widget.App.Chat.Thinking.Chevron" parent="">
        <item name="android:layout_width">18dp</item>
        <item name="android:layout_height">18dp</item>
        <item name="android:layout_marginStart">4dp</item>
        <item name="android:tint">?attr/colorPrimary</item>
    </style>

    <style name="Widget.App.Chat.Thinking.Body" parent="">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">4dp</item>
        <item name="android:textColor">?attr/colorPrimary</item>
        <item name="android:textSize">14sp</item>
    </style>

    <!-- Regenerated-response pager (owner spec, Aug 16 2026): the far-right
         previous/next chevrons and the "n / m" count on an assistant message.
         Colors default to colorPrimary here but ChatAdapter recolors the glyphs
         and count to the bubble foreground so they follow the theme, exactly
         like the other message-action glyphs. The four-plus-versions picker is
         drawn in code as a bare borderless number list (no box, no chevron),
         reusing the shared rounded-surface drawable for its background. -->
    <style name="Widget.App.ResponseVersion.Chevron" parent="">
        <item name="android:layout_width">28dp</item>
        <item name="android:layout_height">28dp</item>
        <item name="android:background">@color/transparent</item>
        <item name="android:padding">2dp</item>
        <item name="android:scaleType">centerInside</item>
        <item name="android:tint">?attr/colorPrimary</item>
    </style>

    <style name="Widget.App.ResponseVersion.Count" parent="">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:gravity">center</item>
        <item name="android:minWidth">40dp</item>
        <item name="android:paddingStart">2dp</item>
        <item name="android:paddingEnd">2dp</item>
        <item name="android:paddingTop">4dp</item>
        <item name="android:paddingBottom">4dp</item>
        <item name="android:textColor">?attr/colorPrimary</item>
        <item name="android:textSize">13sp</item>
    </style>

    <!-- Summoning Circle placement variants. They inherit the canonical
         dropdown visuals and add only spacing for the trailing edit button. -->
    <style name="Widget.App.QuickTile.Label" parent="Widget.App.Dropdown.CanonicalLabel">
        <item name="android:layout_marginStart">16dp</item>
    </style>

    <style name="Widget.App.QuickTile.Value" parent="Widget.App.Dropdown.CanonicalValue">
        <item name="android:layout_marginStart">8dp</item>
        <item name="android:layout_marginEnd">4dp</item>
    </style>

    <!-- Trailing edit button: opens that category's manager screen. -->
    <style name="Widget.App.QuickTile.EditButton" parent="">
        <item name="android:layout_width">40dp</item>
        <item name="android:layout_height">40dp</item>
        <item name="android:layout_marginEnd">10dp</item>
        <item name="android:background">?attr/selectableItemBackgroundBorderless</item>
        <item name="android:padding">8dp</item>
        <item name="android:scaleType">fitCenter</item>
        <item name="android:src">@drawable/ic_edit</item>
        <item name="android:tint">?attr/colorPrimary</item>
    </style>

    <!-- Legacy dropdown styles remain visually unchanged while screens migrate
         one controlled batch at a time. Do not use these for new work. -->
    <style name="Widget.App.Dropdown.Label" parent="">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">?attr/colorPrimary</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="Widget.App.Dropdown.Value" parent="">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginStart">8dp</item>
        <item name="android:background">?attr/selectableItemBackground</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:drawableEnd">@drawable/ic_arrow_drop_down</item>
        <item name="android:drawableTint">?attr/appTextColor</item>
        <item name="android:drawablePadding">2dp</item>
        <item name="android:ellipsize">end</item>
        <item name="android:maxLines">1</item>
        <item name="android:paddingVertical">6dp</item>
        <item name="android:paddingStart">4dp</item>
        <item name="android:textColor">?attr/appTextColor</item>
    </style>

    <!-- Canonical dropdown visual family used by screens already migrated to
         the approved contract. Placement belongs to the containing layout. -->
    <style name="Widget.App.Dropdown.CanonicalLabel" parent="">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">?attr/appDropdownLabelColor</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Width is measured from the longest option by AppDropdown;
         wrap_content is only its safe inflation default. -->
    <style name="Widget.App.Dropdown.CanonicalValue" parent="">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">40dp</item>
        <item name="android:background">@drawable/bg_dropdown_closed</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:drawableEnd">@drawable/ic_chevron_down</item>
        <item name="android:drawableTint">@color/dropdown_text</item>
        <item name="android:drawablePadding">6dp</item>
        <item name="android:ellipsize">end</item>
        <item name="android:gravity">center_vertical|start</item>
        <item name="android:maxLines">1</item>
        <item name="android:paddingStart">12dp</item>
        <item name="android:paddingEnd">8dp</item>
        <item name="android:textColor">@color/dropdown_text</item>
        <item name="android:textSize">14sp</item>
    </style>

    <!-- Fixed-width gallery filter. It preserves the compact floating Filter
         label while resolving the box, chevron, text, and disabled state from
         the same semantic theme roles as the canonical dropdown. -->
    <style name="Widget.App.Dropdown.GalleryFilter" parent="App.ExposedTextField">
        <item name="boxBackgroundColor">?attr/appDropdownBackgroundColor</item>
        <item name="boxCornerRadiusBottomEnd">5dp</item>
        <item name="boxCornerRadiusBottomStart">5dp</item>
        <item name="boxCornerRadiusTopEnd">5dp</item>
        <item name="boxCornerRadiusTopStart">5dp</item>
        <item name="boxStrokeColor">@color/gallery_dropdown_stroke</item>
        <item name="boxStrokeWidth">1dp</item>
        <item name="boxStrokeWidthFocused">1dp</item>
        <item name="endIconDrawable">@drawable/ic_chevron_down</item>
        <item name="endIconTint">@color/dropdown_text</item>
        <item name="hintTextColor">?attr/appDropdownLabelColor</item>
        <item name="android:textColorHint">?attr/appDropdownLabelColor</item>
    </style>

    <style name="Widget.App.Dropdown.Option" parent="">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">40dp</item>
        <item name="android:background">?attr/appDropdownBackgroundColor</item>
        <item name="android:ellipsize">end</item>
        <item name="android:gravity">center_vertical|start</item>
        <item name="android:maxLines">1</item>
        <item name="android:paddingStart">12dp</item>
        <item name="android:paddingEnd">38dp</item>
        <item name="android:textColor">?attr/appDropdownTextColor</item>
        <item name="android:textSize">14sp</item>
    </style>

    <!-- Voice Browser family. All colors resolve through the same semantic
         theme roles as shared dropdowns and rows; the reference screenshot
         supplies hierarchy only and contributes no literal palette values. -->
    <style name="Widget.App.VoiceBrowser.Segment" parent="Widget.Material3.Button.OutlinedButton">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:backgroundTint">@color/voice_browser_segment_background</item>
        <item name="android:textColor">@color/voice_browser_segment_text</item>
        <item name="android:textSize">14sp</item>
        <item name="strokeColor">?attr/appDropdownBorderColor</item>
        <item name="strokeWidth">1dp</item>
    </style>

    <style name="Widget.App.VoiceBrowser.Row" parent="">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">?attr/selectableItemBackground</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:minHeight">72dp</item>
        <item name="android:paddingStart">20dp</item>
        <item name="android:paddingEnd">12dp</item>
        <item name="android:paddingVertical">8dp</item>
    </style>

    <style name="Widget.App.VoiceBrowser.SelectedIcon" parent="">
        <item name="android:layout_width">28dp</item>
        <item name="android:layout_height">28dp</item>
        <item name="android:layout_marginEnd">32dp</item>
        <item name="android:tint">?attr/colorPrimary</item>
    </style>

    <!-- Long-press voice identity editor. Geometry and typography live here so
         the complete popup can be adjusted without changing activity code. -->
    <style name="App.VoiceIdentityDialog" parent="App.MaterialAlertDialog" />

    <style name="Widget.App.VoiceIdentityDialog.Content" parent="">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:orientation">vertical</item>
        <item name="android:paddingStart">24dp</item>
        <item name="android:paddingTop">20dp</item>
        <item name="android:paddingEnd">24dp</item>
        <item name="android:paddingBottom">20dp</item>
    </style>

    <style name="Widget.App.VoiceIdentityDialog.Name" parent="Widget.App.Field.Box">
        <item name="android:minHeight">56dp</item>
        <item name="android:gravity">center_vertical|start</item>
    </style>

    <!-- Preview control sits beside the name field and matches its height so
         the two read as one input row (owner ruling, Aug 26 2026: the preview
         button must be about the same height as the writing box, not a tiny
         square). -->
    <style name="Widget.App.VoiceIdentityDialog.Preview" parent="Widget.Material3.Button.TextButton">
        <item name="android:layout_width">56dp</item>
        <item name="android:layout_height">56dp</item>
        <item name="android:layout_marginStart">12dp</item>
        <item name="android:minWidth">56dp</item>
        <item name="android:minHeight">56dp</item>
        <item name="android:padding">0dp</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="iconPadding">0dp</item>
        <item name="iconTint">?attr/colorPrimary</item>
    </style>

    <style name="Widget.App.VoiceIdentityDialog.SectionLabel" parent="Widget.App.Field.Label">
        <item name="android:layout_marginTop">20dp</item>
    </style>

    <style name="Widget.App.VoiceIdentityDialog.RadioButton" parent="Widget.Material3.CompoundButton.RadioButton">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:buttonTint">?attr/colorPrimary</item>
        <item name="android:textColor">?attr/appTextColor</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="Widget.App.VoiceIdentityDialog.IdentifierValue" parent="Widget.App.Row.Title">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_marginTop">20dp</item>
    </style>

    <style name="Widget.App.VoiceBrowser.Action" parent="Widget.Material3.Button.TextButton">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">56dp</item>
        <item name="android:minWidth">72dp</item>
        <item name="android:paddingHorizontal">8dp</item>
        <item name="android:textColor">?attr/colorPrimary</item>
        <item name="android:textSize">12sp</item>
        <item name="iconTint">?attr/colorPrimary</item>
    </style>

    <!-- Provider chart (Choose Provider screen, owner spec Aug 2 2026): a
         horizontally scrollable table of provider endpoints. One Chart.Row per
         provider (and one for the header), cells sized by the column table in
         ChooseProviderActivity; HeaderCell/Cell carry the shared typography.
         All colors resolve through theme attributes (appTextColor here;
         the Ignore control uses appSubtleTextColor / colorError / colorOnError
         via its drawables and runtime tints) so future palettes restyle the
         whole chart without touching layouts or code. -->
    <style name="Widget.App.Chart.Row" parent="">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:orientation">horizontal</item>
    </style>

    <style name="Widget.App.Chart.HeaderCell" parent="">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:ellipsize">end</item>
        <item name="android:maxLines">1</item>
        <item name="android:paddingEnd">8dp</item>
        <item name="android:textColor">?attr/appTextColor</item>
        <item name="android:textSize">13sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="Widget.App.Chart.Cell" parent="">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:ellipsize">end</item>
        <item name="android:maxLines">1</item>
        <item name="android:paddingEnd">8dp</item>
        <item name="android:textColor">?attr/appTextColor</item>
        <item name="android:textSize">13sp</item>
    </style>

    <!-- Plain full-row checkbox option (owner spec, Aug 2 2026 — the provider
         Filters panel's Tool Support / Implicit Caching / Zero Data Retention
         lines): the whole row toggles the box, but the row must look like a
         normal line of text, NOT a button or tile — no background. Distinct
         from Widget.App.Row.Toggle, which is a switch row with subtitle. -->
    <style name="Widget.App.CheckOption.Row" parent="">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:orientation">horizontal</item>
        <item name="android:paddingVertical">6dp</item>
    </style>

    <style name="Widget.App.CheckOption.Label" parent="">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginStart">8dp</item>
        <item name="android:textColor">?attr/appTextColor</item>
        <item name="android:textSize">15sp</item>
    </style>

    <style name="Widget.App.Field.Box" parent="">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">6dp</item>
        <item name="android:background">@drawable/bg_field_box</item>
        <item name="android:padding">12dp</item>
    </style>

    <!-- A small, right-sized numeric input blank (owner spec, July 23 2026 —
         the per-log "Maximum Logs Saved" / "Maximum Days Saved" fields on
         Alerts, Errors & Logs). Same bg_field_box skin as Widget.App.Field.Box
         so it themes with every other field, but sized to a handful of digits
         and centred rather than stretched full-width: it sits inline at the end
         of its label's line. The DIGIT WIDTH and android:maxLength are set
         per-instance (android:ems) — a 4-digit "Maximum Logs" blank (must fit
         1000) is wider than a 2-digit "Maximum Days" blank — since the style
         can't know how many digits a given field allows. inputType=number is
         baked in here because every use of this style is a whole-number field. -->
    <style name="Widget.App.Field.NumberBlank" parent="">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@drawable/bg_field_box</item>
        <item name="android:paddingHorizontal">10dp</item>
        <item name="android:paddingVertical">8dp</item>
        <item name="android:gravity">center</item>
        <item name="android:inputType">number</item>
        <item name="android:textColor">?attr/appTextColor</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="BottomNavigationView.ActiveIndicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">?attr/colorAccent</item>
    </style>

    <style name="Theme.Preview" parent="Theme.App">
        <item name="android:windowBackground">@color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentTransitions">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@color/transparent</item>
    </style>

    <style name="AssistantWindowTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/AssistantWindowStyle</item>
        <item name="colorPrimary">@color/accent_500</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/accent_100</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="colorAccent">@color/accent_900</item>
        <item name="appRowTitleColor">@color/text</item>
        <item name="appRowSubtitleColor">@color/text_subtitle</item>
        <item name="appTextColor">@color/text</item>
        <item name="appSubtleTextColor">@color/text_subtitle</item>
        <item name="appTitleTextColor">@color/text_title</item>
        <item name="appDropdownBorderColor">@color/dropdown_border</item>
        <item name="appDropdownBackgroundColor">@color/window_background</item>
        <item name="appDropdownTextColor">@color/text</item>
        <item name="appDropdownLabelColor">?attr/colorPrimary</item>
        <item name="appDropdownDisabledTextColor">@color/text_subtitle</item>
        <item name="appDropdownDisabledBorderColor">@color/dropdown_disabled_border</item>
        <item name="android:windowAnimationStyle">@style/AssistantWindowAnimation</item>
        <item name="android:typeface">sans</item>
        <item name="android:fontFamily">@font/default_font</item>
        <item name="android:backgroundDimAmount">0.4</item>
        <item name="android:windowIsFloating">false</item>
    </style>

    <style name="AssistantWindowAnimation" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/assistant_entry</item>
        <item name="android:windowExitAnimation">@anim/assistant_exit</item>
        <item name="android:activityOpenEnterAnimation">@anim/assistant_entry</item>
        <item name="android:activityOpenExitAnimation">@anim/assistant_exit</item>
        <item name="android:activityCloseEnterAnimation">@anim/assistant_entry</item>
        <item name="android:activityCloseExitAnimation">@anim/assistant_exit</item>
        <item name="android:inAnimation">@anim/assistant_entry</item>
        <item name="android:outAnimation">@anim/assistant_exit</item>
        <item name="android:fragmentOpenEnterAnimation">@anim/assistant_entry</item>
        <item name="android:fragmentOpenExitAnimation">@anim/assistant_exit</item>
        <item name="android:fragmentCloseEnterAnimation">@anim/assistant_entry</item>
        <item name="android:fragmentCloseExitAnimation">@anim/assistant_exit</item>
    </style>

    <style name="AssistantWindowStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="UI.Fade" parent="Theme.App">
        <item name="android:windowAnimationStyle">@style/Theme.Anim.Fade.Material</item>
    </style>

    <style name="UI.Material" parent="Theme.App">
        <item name="android:windowAnimationStyle">@style/Theme.Anim.Fade.Material</item>
    </style>

    <style name="Theme.Transparent" parent="@style/Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/shadow</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentTransitions">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">#99000000</item>
        <item name="colorAccent">@color/accent_900</item>
        <item name="colorPrimary">#ff121212</item>
        <item name="colorPrimaryDark">#ff121212</item>
        <item name="appRowTitleColor">@color/text</item>
        <item name="appRowSubtitleColor">@color/text_subtitle</item>
        <item name="appTextColor">@color/text</item>
        <item name="appSubtleTextColor">@color/text_subtitle</item>
        <item name="appTitleTextColor">@color/text_title</item>
        <item name="appDropdownBorderColor">@color/dropdown_border</item>
        <item name="appDropdownBackgroundColor">@color/window_background</item>
        <item name="appDropdownTextColor">@color/text</item>
        <item name="appDropdownLabelColor">?attr/colorPrimary</item>
        <item name="appDropdownDisabledTextColor">@color/text_subtitle</item>
        <item name="appDropdownDisabledBorderColor">@color/dropdown_disabled_border</item>
        <item name="android:typeface">sans</item>
        <item name="android:fontFamily">@font/default_font</item>
    </style>

    <style name="Theme.Anim.Fade.Material" parent="@style/Theme.AppCompat">
        <item name="android:windowEnterAnimation">@anim/fade_in</item>
        <item name="android:windowExitAnimation">@anim/fade_out</item>
        <item name="android:activityOpenEnterAnimation">@anim/fade_in</item>
        <item name="android:activityOpenExitAnimation">@anim/fade_out</item>
        <item name="android:activityCloseEnterAnimation">@anim/fade_in</item>
        <item name="android:activityCloseExitAnimation">@anim/fade_out</item>
    </style>

    <style name="Theme.Anim.Material" parent="@style/Theme.Material3.DayNight.NoActionBar">
        <item name="android:activityOpenEnterAnimation">@anim/mtrl_activity_open_enter</item>
        <item name="android:activityOpenExitAnimation">@anim/mtrl_activity_open_exit</item>
        <item name="android:activityCloseEnterAnimation">@anim/mtrl_activity_close_enter</item>
        <item name="android:activityCloseExitAnimation">@anim/mtrl_activity_close_exit</item>
    </style>

    <!-- ===== Companion prompt tabs (multiple-prompt feature, Aug 2026;
         file-tab shape and colors reworked per owner design Aug 16 2026) =====
         The tabbed prompt area on the Edit Companion screen. Each tab reads
         as an angled file tab: a fixed-width diagonal cut on its trailing
         edge (PromptTabBackground - a plain XML <shape> cannot draw a
         non-rectangular edge, so this is a small Drawable class instead,
         still colored entirely from theme attributes, never hardcoded).
         The inactive tab is transparent with a colorOutline stroke; the
         active tab fills with colorSurfaceContainerHigh - the same tone as
         bg_prompt_editor - so the selected tab visually continues into the
         prompt box beneath it, with bright/bold text carrying the "selected"
         signal instead of a bright accent fill. The add-tab button (+) sits
         at the end of the wrapping row and keeps its own plain square shape
         - it is a small icon control, not a labeled file tab. The editor
         frame wraps the prompt name, three-dot menu, and text field. -->
    <style name="Widget.App.PromptTab" parent="">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">36dp</item>
        <item name="android:maxWidth">160dp</item>
        <item name="android:gravity">center</item>
        <item name="android:paddingHorizontal">14dp</item>
        <item name="android:textColor">?attr/appTextColor</item>
        <item name="android:textSize">14sp</item>
        <item name="android:maxLines">1</item>
        <item name="android:ellipsize">end</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
    </style>

    <style name="Widget.App.PromptTab.Active" parent="Widget.App.PromptTab">
        <item name="android:textColor">?attr/appTitleTextColor</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="Widget.App.PromptTab.Add" parent="">
        <item name="android:layout_width">36dp</item>
        <item name="android:layout_height">36dp</item>
        <item name="android:background">?attr/selectableItemBackgroundBorderless</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">?attr/appTextColor</item>
        <item name="android:textSize">20sp</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
    </style>

    <!-- Every dialog's title is centered (owner ruling, July 21 2026 -
         corrects the July 20 ruling below, which only centered a
         single-question dialog's title via a per-call runtime gravity
         tweak). Baked into the theme via materialAlertDialogTitleTextStyle
         so a dialog built with this theme can never come out left-aligned
         by a call site forgetting to set it - the old runtime
         `dialog.findViewById<TextView>(androidx.appcompat.R.id.alertTitle)
         ?.gravity = Gravity.CENTER` tweak is retired, this replaces it
         everywhere at once. -->
    <style name="App.MaterialAlertDialog.Title" parent="MaterialAlertDialog.Material3.Title.Text">
        <item name="android:gravity">center</item>
        <item name="android:textAlignment">center</item>
    </style>

    <style name="App.MaterialAlertDialog" parent="MaterialAlertDialog.Material3">
        <item name="buttonBarNegativeButtonStyle">@style/App.NegativeButtonStyle</item>
        <item name="buttonBarPositiveButtonStyle">@style/App.PositiveButtonStyle</item>
        <item name="materialAlertDialogTitleTextStyle">@style/App.MaterialAlertDialog.Title</item>
    </style>


    <style name="App.NegativeButtonStyle" parent="Widget.Material3.Button.TextButton.Dialog">
        <item name="android:textColor">?attr/colorAccent</item>
    </style>

    <style name="App.PositiveButtonStyle" parent="Widget.Material3.Button.TextButton.Dialog">
        <item name="android:textColor">?attr/colorAccent</item>
    </style>

    <style name="App.ExposedTextField" parent="Widget.Material3.TextInputLayout.OutlinedBox.ExposedDropdownMenu">
        <item name="materialThemeOverlay">@style/ThemeOverlay.Material3.TextInputEditText.OutlinedBox</item>
    </style>

    <style name="ThemeOverlay.App.BottomSheetDialog" parent="ThemeOverlay.Material3.BottomSheetDialog">
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <!-- Preset color palettes (ui-redesign-plan.md, Phase 0). Each palette is a
         ThemeOverlay applied on top of the activity theme by
         ThemeManager.applyPalette before setContentView. This overlay is
         forced on top of everything else (including the Material dynamic-color
         overlay) as the LAST theme layer applied at runtime, so it is the only
         layer guaranteed to be present when a screen resolves a custom
         attribute like appRowTitleColor/appRowSubtitleColor — defining them on
         Theme.App alone is not enough, since that base layer can sit underneath
         layers that don't carry the attribute forward (this crashed
         CharactersActivity, see git history July 18 2026). Violet reproduces
         the app's original colors unchanged; a future palette overlay — or the
         future saved custom palette (ui-redesign-plan.md Section 4.5) —
         redefines these same items (plus the Material color roles) to retheme
         every shared row, title, hint, and text style with no layout or style
         changes. Every attribute declared in attrs.xml must be defined here. -->
    <style name="ThemeOverlay.Phosphor.Violet" parent="">
        <item name="appRowTitleColor">@color/text</item>
        <item name="appRowSubtitleColor">@color/text_subtitle</item>
        <item name="appTextColor">@color/text</item>
        <item name="appSubtleTextColor">@color/text_subtitle</item>
        <item name="appTitleTextColor">@color/text_title</item>
        <item name="appDropdownBorderColor">@color/dropdown_border</item>
        <item name="appDropdownBackgroundColor">@color/window_background</item>
        <item name="appDropdownTextColor">@color/text</item>
        <item name="appDropdownLabelColor">?attr/colorPrimary</item>
        <item name="appDropdownDisabledTextColor">@color/text_subtitle</item>
        <item name="appDropdownDisabledBorderColor">@color/dropdown_disabled_border</item>
    </style>
</resources>
